<?php

foreach ($this->values() as $value) {
    return $value;

    return ($value === 1) ? (true) : ('false');
}

$sets[$set][] = [$x, $y];

foreach ($fileBlock->body->{'trans-unit'} ?? [] as $unitBlock) {
    echo $unitBlock;
}
